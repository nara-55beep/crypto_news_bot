"""Lucid Strategy Lab page, following the dashboard's inline-page convention."""

LUCID_LAB_HTML = r"""<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Lucid Strategy Lab</title>
<style>
:root{--bg:#070a0f;--panel:#0c1119;--panel2:#101823;--line:#202d3d;--line2:#33445a;--txt:#dbe6f3;--muted:#8392a7;--amber:#f4bd4a;--green:#35d69f;--red:#fb7185;--blue:#60a5fa;--purple:#c084fc;--mono:ui-monospace,SFMono-Regular,Consolas,monospace;--sans:Inter,Segoe UI,Arial,sans-serif}
*{box-sizing:border-box}html{max-width:100%;background:var(--bg);color:var(--txt);scroll-behavior:smooth;overflow-x:hidden}body{max-width:100%;margin:0;font:15.5px/1.65 var(--sans);background:radial-gradient(circle at 82% -10%,rgba(244,189,74,.11),transparent 30rem),var(--bg);overflow-x:hidden}
/* Plain-language layer: every technical block is preceded by a sentence that says
   what it means, so the page can be read without knowing the jargon. */
.plain{margin:0 0 14px;color:#b7c6d8;font-size:15px;max-width:78ch}.plain b{color:#e8f0fa}
.step{display:flex;align-items:center;gap:10px;margin-bottom:4px}.step .n{flex:0 0 auto;width:26px;height:26px;border-radius:50%;display:grid;place-items:center;font:800 12px var(--mono);color:#12100a;background:var(--amber)}
details.more{border:1px solid var(--line);border-radius:12px;background:linear-gradient(145deg,rgba(16,24,35,.96),rgba(10,15,23,.96));margin-bottom:18px;overflow:hidden}details.more>summary{cursor:pointer;padding:16px 18px;font:700 14px var(--sans);color:#dbe6f3;list-style:none;display:flex;align-items:center;gap:10px}details.more>summary::-webkit-details-marker{display:none}details.more>summary:before{content:"▸";color:var(--amber);font-size:13px;transition:transform .15s}details.more[open]>summary:before{transform:rotate(90deg)}details.more>summary:focus-visible{outline:2px solid var(--amber);outline-offset:-2px}details.more>summary small{color:var(--muted);font-weight:400;margin-left:auto;font-size:12px}details.more .inner{padding:0 18px 18px}
.kpi{display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:12px;margin:16px 0 0}.kpi div{border:1px solid #24344a;border-radius:10px;background:#080d14;padding:14px 16px}.kpi .kl{font:700 10px var(--mono);letter-spacing:.09em;text-transform:uppercase;color:var(--muted)}.kpi .kv{font:800 26px/1.1 var(--mono);margin:6px 0 3px}.kpi .kd{font-size:12px;color:#8698ad}
.answer{font-size:17px;line-height:1.6;color:#e6eef8;max-width:80ch;margin:10px 0 0}
.pill.est{border-color:#4a5b74;color:#a9bdd6}
a{color:#8ec5ff}button,input,select{font:inherit}button:focus-visible,input:focus-visible,select:focus-visible,a:focus-visible{outline:2px solid var(--amber);outline-offset:2px}
.top{position:sticky;top:0;z-index:10;display:flex;align-items:center;gap:10px;min-height:52px;padding:9px 18px;border-bottom:1px solid var(--line);background:rgba(7,10,15,.94);backdrop-filter:blur(14px)}
.brand{font:800 15px var(--mono);letter-spacing:.06em;color:#fff}.brand b{color:var(--amber)}.nav{font:12px var(--mono);text-decoration:none;border:1px solid var(--line2);border-radius:6px;padding:6px 10px;color:var(--muted)}.nav:hover{color:#fff;background:var(--panel2)}.spacer{flex:1}.stamp{font:11px var(--mono);color:var(--muted)}
main{width:100%;max-width:1500px;margin:auto;padding:24px}.hero{display:grid;grid-template-columns:minmax(0,1.6fr) minmax(300px,.7fr);gap:18px;margin-bottom:18px}.hero-main,.hero-side,.card{min-width:0;background:linear-gradient(145deg,rgba(16,24,35,.96),rgba(10,15,23,.96));border:1px solid var(--line);border-radius:12px;box-shadow:0 14px 45px rgba(0,0,0,.25)}
.hero-main{padding:26px;position:relative;overflow:hidden}.hero-main:before{content:"";position:absolute;inset:0 auto auto 0;width:100%;height:2px;background:linear-gradient(90deg,var(--amber),var(--purple),transparent)}
.eyebrow{font:700 11px var(--mono);letter-spacing:.16em;text-transform:uppercase;color:var(--amber)}h1{font:700 clamp(28px,4vw,48px)/1.05 var(--sans);margin:8px 0 12px;letter-spacing:-.035em}.lede{max-width:780px;color:#a8b6c8;font-size:15px}.statusline{display:flex;flex-wrap:wrap;gap:8px;margin-top:18px}.pill{display:inline-flex;gap:6px;align-items:center;border:1px solid var(--line2);border-radius:999px;padding:5px 9px;font:11px var(--mono);color:#a9b7ca;background:#0a1018}.pill.warn{border-color:#6b4d19;color:#ffd478}.pill.good{border-color:#14563f;color:#78e7bd}.pill.bad{border-color:#6d2936;color:#ff9daf}
.hero-side{padding:18px}.hero-side h2{margin:0 0 14px;font:700 12px var(--mono);text-transform:uppercase;letter-spacing:.12em;color:var(--muted)}.meta{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1.6fr);gap:9px;border-bottom:1px solid #182231;padding:8px 0}.meta span:first-child{color:var(--muted)}.meta b{min-width:0;font-family:var(--mono);text-align:right;overflow-wrap:anywhere}
.notice{padding:12px 14px;border:1px solid #664b1e;border-left:3px solid var(--amber);border-radius:8px;background:#1b150b;color:#e8cc91;margin:0 0 18px}.notice strong{color:#ffe0a0}.notice.error{border-color:#742c39;border-left-color:var(--red);background:#1a0d12;color:#ffc0ca}
.card{padding:18px;margin-bottom:18px}.card h2{margin:0;font-size:18px}.card h3{margin:18px 0 8px;font-size:14px}.head{display:flex;align-items:flex-start;gap:12px;margin-bottom:16px}.head .num{font:800 11px var(--mono);color:#12100a;background:var(--amber);border-radius:4px;padding:3px 6px}.head p{margin:3px 0 0;color:var(--muted)}
.controls{display:grid;grid-template-columns:repeat(6,minmax(145px,1fr));gap:12px}.field{display:flex;flex-direction:column;gap:5px}.field label{font:700 10px var(--mono);letter-spacing:.08em;text-transform:uppercase;color:var(--muted)}.field input,.field select{width:100%;border:1px solid var(--line2);border-radius:6px;background:#090e15;color:var(--txt);padding:9px}.field small{color:#64748b}.check{justify-content:end}.check label{display:flex;gap:8px;align-items:center;min-height:39px;text-transform:none;font-size:12px}.check input{width:auto}
.metrics{display:grid;grid-template-columns:repeat(6,minmax(120px,1fr));gap:10px}.metric{min-height:86px;padding:12px;border:1px solid #1e2b3a;border-radius:8px;background:#090e15}.metric .k{font:700 10px var(--mono);color:var(--muted);text-transform:uppercase;letter-spacing:.07em}.metric .v{font:800 21px var(--mono);margin-top:6px}.metric .s{font-size:11px;color:#6f8097;margin-top:4px}.metric.good .v{color:var(--green)}.metric.warn .v{color:var(--amber)}.metric.bad .v{color:var(--red)}
.rule-grid,.instruction-grid,.chart-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.rule,.instruction{padding:13px;border:1px solid #1e2b3a;border-radius:8px;background:#090e15}.rule .label,.instruction .sleeve{font:700 10px var(--mono);color:var(--muted);text-transform:uppercase;letter-spacing:.07em}.rule .value{font:800 16px var(--mono);margin:5px 0}.source{display:block;font-size:11px;margin-top:5px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.tag{display:inline-block;border:1px solid #365069;border-radius:4px;padding:1px 5px;font:9px var(--mono);color:#9cc4e8;margin-left:4px}.tag.estimated,.tag.configurable,.tag.ambiguous{border-color:#72551e;color:#f6cf76}.tag.disputed{border-color:#7c2f3c;color:#ff9dad}
.instruction{border-top:2px solid var(--blue)}.instruction p{margin:8px 0;color:#aab7c7}.instruction b{color:#e5edf7}.formula{font:12px/1.7 var(--mono);background:#070b10;border:1px dashed #34445a;border-radius:7px;padding:12px;color:#b8c6d8}
.calc{display:grid;grid-template-columns:minmax(0,1.15fr) minmax(320px,.85fr);gap:16px}.calc-inputs{display:grid;grid-template-columns:repeat(3,minmax(120px,1fr));gap:10px}.calc-result{border:1px solid #2c4259;border-radius:9px;background:#08101a;padding:16px}.quantity{font:900 50px/1 var(--mono);color:var(--green);margin:8px 0}.btn{border:1px solid var(--amber);border-radius:6px;background:#211807;color:#ffd985;padding:9px 14px;font:700 12px var(--mono);cursor:pointer}.btn:hover{background:#2e2109}.btn.secondary{border-color:var(--line2);background:#0b1119;color:#b7c6d8}.btn.danger{border-color:#703242;background:#1d0c12;color:#ffadba}.btn:disabled{opacity:.45;cursor:not-allowed}.actions{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-top:12px}.progress{height:7px;flex:1;min-width:150px;background:#182230;border-radius:99px;overflow:hidden}.progress i{display:block;height:100%;background:linear-gradient(90deg,var(--amber),var(--green));width:0;transition:width .2s}
.plan{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.plan-step{border-left:2px solid var(--amber);padding:4px 12px}.plan-step b{font:700 11px var(--mono);color:var(--amber);text-transform:uppercase}.plan-step ul{padding-left:17px;color:#9eacc0}.plan-step li{margin:6px 0}
.table-wrap{width:100%;max-width:100%;overflow:auto;border:1px solid var(--line);border-radius:8px}table{width:100%;border-collapse:collapse;min-width:760px;background:#090e15}th,td{text-align:left;border-bottom:1px solid #192434;padding:9px 11px;vertical-align:top}th{position:sticky;top:0;background:#101823;color:#8494aa;font:700 10px var(--mono);letter-spacing:.06em;text-transform:uppercase}td.num{text-align:right;font-family:var(--mono)}tr:last-child td{border-bottom:0}.selected-row{background:rgba(53,214,159,.06)}
.chart{min-height:250px;padding:12px;border:1px solid #1d2a39;border-radius:8px;background:#080d14}.chart h3{font:700 11px var(--mono);text-transform:uppercase;color:#9aabc0;margin:0 0 6px}.chart svg{width:100%;height:170px;display:block;overflow:visible}.chart-summary{font-size:11px;color:#78899f}.bars{height:170px;display:flex;align-items:flex-end;gap:5px;border-bottom:1px solid #334155;padding:8px}.bar{flex:1;min-width:5px;background:linear-gradient(var(--green),#176b53);border-radius:3px 3px 0 0;position:relative}.bar:hover:after{content:attr(data-tip);position:absolute;bottom:100%;left:50%;transform:translateX(-50%);background:#101827;border:1px solid var(--line2);padding:4px 6px;white-space:nowrap;font:10px var(--mono);z-index:3}.legend{display:flex;gap:12px;flex-wrap:wrap;font:10px var(--mono);color:var(--muted)}.legend i{display:inline-block;width:8px;height:8px;border-radius:50%;margin-right:4px}
.bar-values{margin:8px 0 0;padding:0;list-style:none;display:grid;grid-template-columns:repeat(auto-fill,minmax(150px,1fr));gap:2px 12px;font:11px var(--mono);color:#8fa0b6}.bar-values li{display:flex;justify-content:space-between;gap:8px;border-bottom:1px dotted #1e2a38;padding:1px 0}.bar-values b{color:#cddaea;font-weight:700}
.verdict{border:1px solid #7c2f3c;border-left:5px solid var(--red);border-radius:10px;background:linear-gradient(145deg,#1d0d12,#150a0e);padding:18px 20px;margin:0 0 18px}.verdict.go{border-color:#14563f;border-left-color:var(--green);background:linear-gradient(145deg,#0b1a15,#08130f)}.verdict h2{margin:0 0 6px;font-size:20px;display:flex;align-items:center;gap:10px;flex-wrap:wrap}.verdict .tagline{font:800 11px var(--mono);letter-spacing:.12em;text-transform:uppercase;border:1px solid currentColor;border-radius:4px;padding:4px 8px;color:var(--red)}.verdict.go .tagline{color:var(--green)}.verdict p{margin:8px 0 0;color:#e2c3c9;max-width:80ch}.verdict.go p{color:#b7e5d0}.gatelist{margin:12px 0 0;padding:0;list-style:none;display:grid;grid-template-columns:repeat(auto-fill,minmax(280px,1fr));gap:6px}.gatelist li{display:flex;gap:8px;align-items:flex-start;font-size:12px;color:#c9b4b9;border-top:1px solid #332026;padding:6px 0}.verdict.go .gatelist li{color:#a8ceba;border-top-color:#17342a}.gatelist .mark{font:700 10px var(--mono);letter-spacing:.06em;flex:0 0 auto;padding:1px 5px;border:1px solid currentColor;border-radius:3px}.gatelist .mark.fail{color:var(--red)}.gatelist .mark.pass{color:var(--green)}
.metric .flag{display:inline-block;font:700 9px var(--mono);letter-spacing:.08em;border:1px solid currentColor;border-radius:3px;padding:0 4px;margin-left:6px;vertical-align:middle}.metric.good .flag{color:var(--green)}.metric.warn .flag{color:var(--amber)}.metric.bad .flag{color:var(--red)}
/* Evaluation planner and prohibited-activity monitors */
.planner{display:grid;grid-template-columns:minmax(0,1fr) minmax(320px,.9fr);gap:18px}.planner-inputs{display:grid;grid-template-columns:repeat(3,minmax(120px,1fr));gap:12px;align-content:start}
.verdict-line{border:1px solid #2c4259;border-radius:10px;background:#08101a;padding:16px}.verdict-line .headline{font:800 19px/1.35 var(--sans);margin:0 0 10px}.verdict-line .headline.ok{color:var(--green)}.verdict-line .headline.no{color:var(--red)}
.checks{list-style:none;padding:0;margin:0;display:grid;gap:8px}.checks li{border:1px solid #1e2b3a;border-radius:8px;background:#090e15;padding:11px 13px}.checks .row{display:flex;align-items:center;gap:9px;flex-wrap:wrap}.checks .name{font-weight:700;color:#e2ebf6}.checks .st{font:700 10px var(--mono);letter-spacing:.08em;border:1px solid currentColor;border-radius:3px;padding:1px 6px}.checks .st.ok{color:var(--green)}.checks .st.bad{color:var(--red)}.checks .meas{font-size:12px;color:#8698ad;margin-top:5px}.checks .val{margin-left:auto;font:800 15px var(--mono)}
.bar-meter{height:7px;border-radius:99px;background:#182230;margin-top:8px;overflow:hidden}.bar-meter i{display:block;height:100%;background:var(--green)}.bar-meter.bad i{background:var(--red)}
@media(max-width:900px){.planner{grid-template-columns:1fr}.planner-inputs{grid-template-columns:repeat(2,1fr)}}
.method{columns:2;column-gap:28px}.method p,.method li{color:#9baabd}.method h3{break-after:avoid}.source-list{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.source-item{padding:9px;border:1px solid #1e2a39;border-radius:6px;background:#090e15}.source-item a{font-weight:700}.source-item small{display:block;color:#718198;margin-top:3px}
.upload{display:flex;align-items:end;gap:10px;flex-wrap:wrap}.upload .field{min-width:240px;flex:1}.empty{padding:24px;text-align:center;color:var(--muted);border:1px dashed var(--line2);border-radius:8px}.skeleton{height:80px;border-radius:8px;background:linear-gradient(90deg,#0c121b,#172231,#0c121b);background-size:220% 100%;animation:pulse 1.3s linear infinite}@keyframes pulse{to{background-position:-220% 0}}
.paper-card{position:relative;overflow:hidden;border-color:#26664f;box-shadow:0 0 0 1px rgba(53,214,159,.08),0 14px 45px rgba(0,0,0,.25)}.paper-card:before{content:"";position:absolute;inset:0 0 auto;height:2px;background:linear-gradient(90deg,transparent,var(--green),var(--blue),transparent);background-size:200% 100%;animation:paperPulse 3s linear infinite}@keyframes paperPulse{to{background-position:-200% 0}}
.paper-toolbar{display:flex;align-items:center;gap:9px;flex-wrap:wrap;margin-left:auto}.paper-health{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin:12px 0}.health{padding:10px;border:1px solid #1e2b3a;border-radius:7px;background:#080d14}.health b{display:block;font:700 10px var(--mono);color:var(--muted);text-transform:uppercase;margin-bottom:4px}.health span{font-size:12px;overflow-wrap:anywhere}.paper-sleeves{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px;margin:14px 0}.paper-sleeve{padding:14px;border:1px solid #28543f;border-radius:9px;background:linear-gradient(155deg,#091710,#090e15)}.paper-sleeve h3{margin:2px 0 8px}.paper-sleeve .sleeve-state{min-height:38px;color:#9cb0c3}.paper-sleeve .meta{font-size:12px}.paper-log{max-height:210px;overflow:auto;border:1px solid var(--line);border-radius:8px;background:#080d14;padding:8px 12px}.paper-log div{display:grid;grid-template-columns:90px 1fr;gap:9px;padding:5px 0;border-bottom:1px solid #15202d;font:11px var(--mono)}.paper-log div:last-child{border:0}.paper-log time{color:#718198}.paper-log .win{color:var(--green)}.paper-log .loss{color:var(--red)}.paper-log .warn{color:var(--amber)}
footer{max-width:1500px;margin:0 auto;padding:8px 24px 40px;color:#627086;font-size:11px}
@media(max-width:1120px){.hero{grid-template-columns:1fr}.controls{grid-template-columns:repeat(3,1fr)}.metrics{grid-template-columns:repeat(3,1fr)}.plan{grid-template-columns:repeat(2,1fr)}.paper-sleeves{grid-template-columns:1fr}}
@media(max-width:820px){main{padding:14px}.top{flex-wrap:wrap}.stamp{display:none}.rule-grid,.instruction-grid,.chart-grid{grid-template-columns:1fr}.calc{grid-template-columns:1fr}.controls,.calc-inputs{grid-template-columns:repeat(2,1fr)}.method{columns:1}.source-list{grid-template-columns:1fr}}
@media(max-width:560px){.hero-main{padding:18px}.controls,.calc-inputs,.metrics,.plan{grid-template-columns:minmax(0,1fr)}.metric{min-height:72px}.top{padding:8px 10px}.brand{width:100%}.card{padding:14px}.quantity{font-size:40px}.hero-side .meta{grid-template-columns:1fr;gap:2px}.hero-side .meta b{text-align:left}.notice,h1,.lede,.formula{overflow-wrap:anywhere}}
@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}.skeleton,.paper-card:before{animation:none}.progress i{transition:none}}
</style>
</head>
<body>
<nav class="top" aria-label="Primary">
  <span class="brand"><b>LUCID</b> STRATEGY LAB</span>
  <a class="nav" href="/">← Chart</a><a class="nav" href="/paper">Paper Trading</a>
  <span class="spacer"></span><span class="stamp" id="topstamp">loading verified rules…</span>
</nav>
<main>
  <section class="hero" aria-labelledby="page-title">
    <div class="hero-main"><div class="eyebrow">Rule-aware futures evaluation research</div><h1 id="page-title">Could this strategy pass a Lucid evaluation?</h1><p class="lede">Pick your account and this page shows the exact rules you have to respect, what the strategy does, whether it actually worked in testing, how much you can safely risk today, and whether your trading is about to break a rule that closes the account. The honest answer to the headline question is in the box below — read that first.</p><div class="statusline" id="statusline"><span class="pill">loading evidence…</span></div></div>
    <aside class="hero-side" aria-label="Research metadata" id="hero-meta"><div class="skeleton"></div></aside>
  </section>
  <section class="verdict" id="verdict" aria-labelledby="verdict-title"><h2 id="verdict-title">Reading the research verdict…</h2></section>
  <div class="notice" id="truth">Loading the source-stamped validation artifact…</div>

  <section class="card" id="account-section"><div class="head"><span class="num">01</span><div><h2>Pick the account you are trading</h2><p class="plain">Choose the Lucid program and account size. Every rule and number below changes with this choice, and the research evidence is only reused when it genuinely covers that combination.</p></div></div><div class="controls">
    <div class="field"><label for="program">Program</label><select id="program"></select></div>
    <div class="field"><label for="stage">Stage</label><select id="stage"><option value="evaluation">Evaluation</option></select></div>
    <div class="field"><label for="size">Account size</label><select id="size"></select></div>
    <div class="field"><label for="instrument">Contract</label><select id="instrument"><option value="MNQ">MNQ · micro</option><option value="MES">MES · micro</option><option value="MCL">MCL · micro</option><option value="NQ">NQ · mini</option><option value="ES">ES · mini</option><option value="CL">CL · mini</option></select></div>
    <div class="field"><label for="horizon">Evaluation horizon</label><select id="horizon"><option value="20">20 sessions</option><option value="30">30 sessions</option><option value="45" selected>45 sessions</option></select></div>
    <div class="field"><label for="preset">Execution stress</label><select id="preset"></select></div>
    <div class="field"><label for="starting-balance">Starting balance</label><input id="starting-balance" readonly></div>
    <div class="field"><label for="selector-current">Current balance</label><input id="selector-current" type="number" step="0.01"></div>
    <div class="field"><label for="selector-remaining">Remaining drawdown</label><input id="selector-remaining" type="number" step="0.01" min="0"></div>
    <div class="field" id="drawdown-wrap"><label for="drawdown">Evaluation drawdown</label><select id="drawdown"><option value="eod">End of day</option><option value="intraday">Intraday</option></select></div>
    <div class="field check" id="dll-wrap"><label><input type="checkbox" id="dll" checked> Daily loss limit enabled</label></div>
  </div><div id="scope-note" class="notice" style="margin-top:14px"></div></section>

  <section class="card"><div class="head"><span class="num">02</span><div><h2>The rules you must not break</h2><p class="plain">Every value here comes from Lucid's own help centre and links back to the exact page. A tag marks anything that is <b>estimated</b> or <b>ambiguous</b> rather than officially published.</p></div></div><div class="rule-grid" id="rule-grid"><div class="skeleton"></div></div><h3>Prohibited and operational behavior</h3><div class="formula" id="behavior"></div></section>

  <section class="card"><div class="head"><span class="num">03</span><div><h2>What the strategy actually does</h2><p class="plain">Three independent setups, each with an exact numeric trigger. Nothing is discretionary: a signal is only taken after the bar that produced it has closed, and entry is the next minute's open.</p></div></div><div class="instruction-grid" id="instructions"></div><h3>Portfolio risk rules</h3><div class="formula" id="portfolio-rules"></div></section>

  <section class="card"><div class="head"><span class="num">04</span><div><h2>Did it actually work?</h2><p class="plain" id="backtest-caption">Every attempt counts, including the ones that simply ran out of days. Nothing is dropped to flatter the result.</p></div></div><div class="metrics" id="backtest-metrics"></div><h3>Development / validation / chronological test</h3><div class="table-wrap"><table><thead><tr><th>Split</th><th>Windows</th><th>Pass</th><th>Breach</th><th>Unfinished</th><th>Median pass*</th><th>Worst DD</th></tr></thead><tbody id="split-table"></tbody></table></div><small>* Conditional on passing. Restricted-mean duration is shown in the metric details.</small></section>

  <section class="card paper-card" id="paper-runtime"><div class="head"><span class="num">05</span><div><h2>Forward paper account</h2><p class="plain">The three strategy sleeves run continuously inside one shared LucidPro 25K simulation so you can watch them behave in real time. This is a paper account: <b>no broker order can be sent from this page.</b></p></div><div class="paper-toolbar"><span class="pill good" id="paper-status">loading</span><button class="btn secondary" id="paper-toggle" type="button">Pause entries</button><button class="btn danger" id="paper-reset" type="button">Reset paper account</button></div></div>
    <div class="notice" id="paper-truth">Loading the paper runtime...</div>
    <div class="metrics" id="paper-metrics"><div class="skeleton"></div></div>
    <div class="paper-health"><div class="health"><b>Futures feed</b><span id="paper-feed">loading</span></div><div class="health"><b>Exchange calendar</b><span id="paper-calendar">loading</span></div><div class="health"><b>Event filter / evidence</b><span id="paper-events">loading</span></div></div>
    <div class="paper-sleeves" id="paper-sleeves"><div class="skeleton"></div></div>
    <h3>Open paper positions</h3><div class="table-wrap"><table><thead><tr><th>Sleeve</th><th>Side / quantity</th><th>Entry</th><th>Stop</th><th>Target</th><th>Unrealized</th><th>Reserved risk</th></tr></thead><tbody id="paper-positions"></tbody></table></div>
    <h3>Closed paper trades</h3><div class="table-wrap"><table><thead><tr><th>Closed</th><th>Sleeve</th><th>Side / quantity</th><th>Entry</th><th>Exit</th><th>Net P&amp;L</th><th>Reason</th><th>Evidence</th></tr></thead><tbody id="paper-history"></tbody></table></div>
    <h3>Paper activity</h3><div class="paper-log" id="paper-log"><div><span>--</span><span>waiting for runtime</span></div></div>
  </section>

  <section class="card"><div class="head"><span class="num">06</span><div><h2>Can I still make it from here?</h2><p class="plain">Mid-evaluation the useful question is not what happened historically, but what is still required and how much you can risk before the drawdown floor ends it. This is pure arithmetic on your account's official numbers, so it holds even though the historical edge is unproven.</p></div></div><div class="planner"><div class="planner-inputs">
    <div class="field"><label for="plan-balance">Balance now</label><input id="plan-balance" type="number" step="0.01"></div>
    <div class="field"><label for="plan-floor">Drawdown floor now</label><input id="plan-floor" type="number" step="0.01"></div>
    <div class="field"><label for="plan-sessions">Trading days left</label><input id="plan-sessions" type="number" min="1" step="1" value="20"></div>
    <div class="field"><label for="plan-trades">Trades per day</label><input id="plan-trades" type="number" min="1" step="1" value="3"></div>
    <div class="field"><label for="plan-streak">Losing streak to survive</label><input id="plan-streak" type="number" min="1" step="1" value="3"><small>Size so this many losses in a row cannot breach.</small></div>
    <div class="field"><label for="plan-rr">Reward per unit risk</label><input id="plan-rr" type="number" min="0.1" step="0.1" value="2"></div>
    <div class="actions"><button class="btn" id="plan-btn">Work it out</button></div>
  </div><div class="verdict-line" id="plan-result"><div class="empty">Enter where the account stands, then work it out.</div></div></div></section>

  <section class="card"><div class="head"><span class="num">07</span><div><h2>Am I about to get the account closed?</h2><p class="plain">Breaking a prohibited-activity rule forfeits profits and can close the account, which ends an evaluation just as surely as the drawdown floor. Paste your fills below, or let the forward paper runtime fill them in, and each published rule is scored.</p></div></div>
  <div class="upload" style="margin-bottom:14px"><div class="field" style="flex:1;min-width:260px"><label for="compliance-input">Trades as JSON</label><input id="compliance-input" value='[{"entry":"2026-08-17T13:45:00-04:00","exit":"2026-08-17T13:45:03-04:00","net_pnl":300,"instrument":"MNQ","side":"long"}]'></div><button class="btn secondary" id="compliance-btn">Score my activity</button><button class="btn secondary" id="compliance-paper-btn">Use paper runtime</button></div>
  <ul class="checks" id="compliance-result"><li><div class="empty">No activity scored yet.</div></li></ul></section>

  <section class="card"><div class="head"><span class="num">08</span><div><h2>How many contracts can I take?</h2><p class="plain">Enter where the account stands and this returns the largest size that still respects your drawdown room, the daily loss limit, execution costs, your safety reserve and Lucid's contract cap. It will not return a size that could push you through the floor.</p></div></div><div class="calc"><div class="calc-inputs">
    <div class="field"><label for="current">Current balance</label><input id="current" type="number" step="0.01"></div>
    <div class="field"><label for="floor">Drawdown floor</label><input id="floor" type="number" step="0.01"></div>
    <div class="field"><label for="daily-room">Daily loss remaining</label><input id="daily-room" type="number" step="0.01" placeholder="not applicable"></div>
    <div class="field"><label for="stop-ticks">Stop distance (ticks)</label><input id="stop-ticks" type="number" min="1" step="1" value="40"></div>
    <div class="field"><label for="risk-budget">Selected risk budget</label><input id="risk-budget" type="number" min="1" step="1" value="400"></div>
    <div class="field"><label for="reserve">Safety reserve</label><input id="reserve" type="number" min="0" step="1" value="100"></div>
    <div class="field"><label for="open-cap">Open micro equivalents</label><input id="open-cap" type="number" min="0" step="1" value="0"></div>
    <div class="field"><label for="committed-risk">Committed stop risk</label><input id="committed-risk" type="number" min="0" step="0.01" value="0"></div>
    <div class="actions"><button class="btn" id="calc-btn">Calculate size</button></div>
  </div><div class="calc-result" id="calc-result"><div class="empty">Enter the current account values, then calculate.</div></div></div></section>

  <section class="card"><div class="head"><span class="num">09</span><div><h2>What to do each day</h2><p class="plain">A checklist generated from the rules and strategy above rather than hand-written, so it always matches the account you selected.</p></div></div><div class="plan" id="daily-plan"></div></section>

  

  <details class="more"><summary>What if fills are worse than modelled?<small>stress tests under worse costs</small></summary><div class="inner"><p class="plain">Each row re-runs the whole evaluation with worse costs. If the result only survives the friendliest assumptions, it is not a result.</p><div class="table-wrap"><table><thead><tr><th>Scenario</th><th>Windows</th><th>Pass</th><th>Breach</th><th>Unfinished</th><th>Median pass*</th><th>Worst drawdown</th></tr></thead><tbody id="stress-table"></tbody></table></div><div class="actions"><button class="btn" id="replay-btn">Load stored scenario</button><button class="btn danger" id="cancel-btn" disabled>Cancel</button><div class="progress" id="progress-track" role="progressbar" aria-label="Scenario loading progress" aria-valuemin="0" aria-valuemax="100" aria-valuenow="0"><i id="progress"></i></div><span class="stamp" id="run-state">idle</span></div></div></details>

  <details class="more"><summary>The results as pictures<small>charts with text values</small></summary><div class="inner"><p class="plain">Every chart lists its values as text as well, so nothing depends on hovering a mouse.</p><div class="chart-grid">
    <div class="chart"><h3>Representative equity path</h3><svg id="equity-chart" role="img"></svg><p class="chart-summary" id="equity-summary"></p></div>
    <div class="chart"><h3>Drawdown room</h3><svg id="drawdown-chart" role="img"></svg><p class="chart-summary" id="drawdown-summary"></p></div>
    <div class="chart"><h3>Rolling pass probability</h3><svg id="rolling-chart" role="img"></svg><p class="chart-summary" id="rolling-summary"></p></div>
    <div class="chart"><h3>Pass-time distribution</h3><div class="bars" id="duration-bars" role="img"></div><p class="chart-summary" id="duration-summary"></p></div>
    <div class="chart"><h3>Pass / breach / unfinished</h3><div class="bars" id="outcome-bars" role="img"></div><p class="chart-summary" id="outcome-summary"></p></div>
    <div class="chart"><h3>Monte Carlo terminal profit</h3><div class="bars" id="terminal-bars" role="img"></div><p class="chart-summary" id="terminal-summary"></p></div>
    <div class="chart"><h3>Modeled cost breakdown</h3><div class="bars" id="cost-bars" role="img"></div><p class="chart-summary" id="cost-summary"></p></div>
  </div></div></details>

  <details class="more"><summary>A single evaluation, session by session<small>day-by-day account timeline</small></summary><div class="inner"><p class="plain">One representative historical run near the median, showing the balance, the moving drawdown floor and how much room was left each day.</p><div class="table-wrap"><table><thead><tr><th>Day</th><th>Session</th><th>Start</th><th>End</th><th>Daily net</th><th>Largest day</th><th>Consistency</th><th>MLL floor</th><th>Room</th><th>Permitted</th><th>Status / warning</th></tr></thead><tbody id="timeline"></tbody></table></div></div></details>

  <details class="more"><summary>How other strategies compared<small>candidate ranking and sensitivity</small></summary><div class="inner"><p class="plain">Candidates are ranked on how often they passed before breaching, not on raw profit. Rejected versions stay on the list.</p><div class="table-wrap"><table><thead><tr><th>Candidate / version</th><th>Instrument</th><th>Trades</th><th>Windows</th><th>Expectancy</th><th>PF</th><th>Pass</th><th>Severe pass</th><th>Status / reason</th></tr></thead><tbody id="candidates"></tbody></table></div><h3>Risk sensitivity</h3><div class="chart"><div class="bars" id="sensitivity-bars" role="img"></div><p class="chart-summary" id="sensitivity-summary"></p></div><h3>Signal-parameter sensitivity</h3><div class="table-wrap"><table><thead><tr><th>Dimension</th><th>Value</th><th>Selected</th><th>Trades</th><th>Expectancy</th><th>Pass</th><th>Breach</th><th>Unfinished</th></tr></thead><tbody id="signal-sensitivity"></tbody></table></div></div></details>

  <details class="more"><summary>Check your own market data<small>CSV / Parquet validator</small></summary><div class="inner"><p class="plain">Upload a CSV or Parquet file and this reports duplicates, gaps, timezone problems, impossible prices and rollover ambiguity. Nothing is uploaded to the evidence set and files are not retained.</p><form class="upload" id="upload-form"><div class="field"><label for="data-file">Market data file</label><input id="data-file" type="file" accept=".csv,.parquet,.pq" required></div><div class="field"><label for="data-symbol">Expected symbol</label><input id="data-symbol" value="MNQ" required></div><button class="btn secondary" type="submit">Validate only</button></form><div id="upload-result" class="empty" style="margin-top:12px">No file selected. This validator never adds uploaded data to the evidence artifact.</div></div></details>

  <details class="more"><summary>Sources and full method<small>official links, gates and methodology</small></summary><div class="inner"><p class="plain">Direct links to every official rule page used, plus the assumptions and unresolved conflicts behind the numbers.</p><div class="method" id="methodology"></div><h3>Official sources used by the active configuration</h3><div class="source-list" id="sources"></div></div></details>
</main>
<footer id="footer">Lucid Strategy Lab combines research artifacts with a forward paper simulation. It has no live order routing and cannot guarantee an evaluation outcome or profit.</footer>
<script>
'use strict';
const $=id=>document.getElementById(id); let state=null,paperState=null,activeJob=null,pollTimer=null;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=v=>v==null?'—':new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:2}).format(Number(v));
const pct=v=>v==null?'—':(Number(v)*100).toFixed(1)+'%'; const num=v=>v==null?'—':Number(v).toLocaleString();
function query(){return new URLSearchParams({program:$('program').value||'lucidpro',stage:'evaluation',size:$('size').value||'25000',daily_drawdown:$('drawdown').value,daily_loss_enabled:$('dll').checked?'true':'false'}).toString()}
async function getJSON(url,opts){const r=await fetch(url,opts);let j;try{j=await r.json()}catch(e){throw new Error('Server returned non-JSON response')}if(!r.ok||j.ok===false)throw new Error(j.error||('HTTP '+r.status));return j}
function programChanged(){if(!state)return;const p=state.program_options.find(x=>x.id===$('program').value);$('size').innerHTML=p.sizes.map(v=>`<option value="${v}">${money(v).replace('.00','')}</option>`).join('');$('drawdown-wrap').style.display=p.id==='luciddaily'?'flex':'none';$('dll-wrap').style.display=(p.id==='luciddaily'||p.id==='lucidpro')?'flex':'none';loadSnapshot()}
async function loadSnapshot(){try{$('truth').className='notice';$('truth').textContent='Loading verified rules and the source-stamped evidence artifact…';state=await getJSON('/api/lucid-lab/snapshot?'+query());render()}catch(e){$('truth').className='notice error';$('truth').textContent='Lucid Lab could not load: '+e.message}}
function initControls(){const currentProgram=$('program').value||'lucidpro';$('program').innerHTML=state.program_options.map(p=>`<option value="${p.id}">${esc(p.label)}</option>`).join('');$('program').value=currentProgram;const p=state.program_options.find(x=>x.id===currentProgram)||state.program_options[0];const currentSize=$('size').value||String(state.account.account_size);$('size').innerHTML=p.sizes.map(v=>`<option value="${v}">${money(v).replace('.00','')}</option>`).join('');$('size').value=p.sizes.includes(Number(currentSize))?currentSize:String(p.sizes[0]);$('preset').innerHTML=state.execution_presets.map(p=>`<option value="${p.key}">${esc(p.label)}</option>`).join('');$('preset').value=$('preset').dataset.keep||'normal';$('drawdown-wrap').style.display=p.id==='luciddaily'?'flex':'none';$('dll-wrap').style.display=(p.id==='luciddaily'||p.id==='lucidpro')?'flex':'none'}
function render(){initControls();const e=state.evidence,a=state.account,s=e.selected,d=e.data;$('topstamp').textContent='rules checked '+state.rules_last_checked+' · run '+e.run_id;$('statusline').innerHTML=`<span class="pill warn">${esc(e.status_label)}</span><span class="pill">${esc(s.strategy_version)}</span><span class="pill">run ${esc(e.run_id)}</span><span class="pill good">forward paper runtime</span><span class="pill">no live order routing</span>`;$('hero-meta').innerHTML=`<h2>Research snapshot</h2>${[['Strategy',s.strategy_version],['Rules verified',state.rules_last_checked],['Data',d.test_start+' → '+d.test_end],['Program',s.program+' '+money(s.account_size).replace('.00','')],['Instrument',s.instrument],['Simulation',e.generated_at]].map(x=>`<div class="meta"><span>${esc(x[0])}</span><b>${esc(x[1])}</b></div>`).join('')}`;$('truth').innerHTML=`<strong>What this page is.</strong> ${esc(state.disclaimer)} ${esc(s.limitations[0])}`;$('scope-note').className='notice'+(state.evidence_applies?'':' error');$('scope-note').textContent=state.evidence_scope_note;renderVerdict();renderRules();renderInstructions();setCalculatorDefaults();renderPlan();renderBacktest();renderStress();renderCharts();renderTimeline();renderCandidates();renderMethod()}
function renderVerdict(){const e=state.evidence,v=e.verdict,cs=e.candidate_search,go=String(e.status||'').toUpperCase()==='GO';const box=$('verdict');box.className='verdict'+(go?' go':'');
  const gates=(v.validation_gates||[]).map(g=>`<li><span class="mark ${g.passed?'pass':'fail'}">${g.passed?'MET':'NOT MET'}</span><span>${esc(g.detail)}</span></li>`).join('');
  const failed=(v.validation_gates||[]).filter(g=>!g.passed).length;
  const searchLine=cs?` A separate pre-registered search of ${num(cs.variants_tested)} strategy variants reached ${pct(cs.best_candidate_holdout_pass_rate)} on ${num(cs.holdout_blocks)} disjoint holdout blocks and ${cs.beat_frozen_baseline?'beat':'did not beat'} the frozen baseline at ${pct(cs.frozen_baseline_holdout_pass_rate)}, so it does not change this verdict.`:'';
  box.innerHTML=`<h2><span class="tagline">${esc(v.decision)}</span> ${esc(e.status_label)}</h2><p>${esc(v.reason)}${searchLine}</p><p><b>${failed} of ${(v.validation_gates||[]).length} evidence gates are not met.</b> Every gate must be met before this research could support a decision; it can never enable order routing.</p><ul class="gatelist">${gates}</ul>`}
function sourceFor(i=0){return state.account.sources[i]||state.account.sources[0]}
function rule(label,value,source,tag='verified',note=''){return `<div class="rule"><div class="label">${esc(label)} <span class="tag ${tag}">${esc(tag)}</span></div><div class="value">${esc(value)}</div>${note?`<div>${esc(note)}</div>`:''}<a class="source" href="${esc(source.url)}" target="_blank" rel="noopener">${esc(source.title)} · ${esc(source.displayed_date)}</a></div>`}
function metaRule(key,label,value,note=''){const m=state.account.rule_metadata[key];return rule(label,value,m.source,m.confidence,note||m.notes)}
function renderRules(){const a=state.account;$('rule-grid').innerHTML=[metaRule('profit_target','Profit target',money(a.profit_target)),metaRule('maximum_loss','Maximum loss',money(a.max_loss)),metaRule('drawdown_type','Drawdown',a.drawdown_type.toUpperCase()+' trailing'),metaRule('daily_loss_limit','Daily loss limit',a.daily_loss_limit?money(a.daily_loss_limit)+' soft stop':'None'),metaRule('consistency_limit','Consistency',a.consistency_limit_pct?a.consistency_limit_pct+'% (strict; cushion ignored)':'None in evaluation'),metaRule('maximum_contracts','Maximum contracts',a.max_minis+' minis or '+a.max_micros+' micros'),metaRule('scaling','Scaling rule','Fixed evaluation cap'),metaRule('minimum_trading_days','Minimum days',String(a.minimum_trading_days)),metaRule('forced_close','Official cutoff',a.forced_close_ny+' New York','Selected strategy exits by 16:00.'),metaRule('overnight','Overnight',a.overnight_allowed?'Permitted inside the official session; strategy stays flat':'Blocked by conservative model'),metaRule('weekend','Weekend','Not permitted'),metaRule('commission_micro','MNQ/MES/MCL commission','$0.50 per side'),metaRule('news','News rule',a.news_rule),metaRule('locked_floor','Trail lock',money(a.locked_floor)+' after > '+money(a.trail_trigger))].join('');$('behavior').innerHTML=`No martingale · no averaging recovery · no cross-account hedging · no HFT · no simulated-fill exploitation · no microscalping · aggregate mini/micro cap enforced.<br>${a.conflicts.map(x=>'⚠ '+esc(x)).join('<br>')||'No unresolved program-specific conflict changes this simulation.'}`}
function renderInstructions(){const s=state.evidence.selected;$('instructions').innerHTML=state.evidence.strategy_rules.map(r=>`<article class="instruction"><div class="sleeve">${esc(r.sleeve)}</div><p><b>Setup:</b> ${esc(r.setup)}</p><p><b>Entry:</b> ${esc(r.entry)}</p><p><b>Stop:</b> ${esc(r.stop)}</p><p><b>Target:</b> ${esc(r.target)}</p></article>`).join('');$('portfolio-rules').innerHTML=`Morning sleeve budget ${money(s.morning_risk_usd)} · prior-range budget ${money(s.prior_breakout_risk_usd)} · ${s.maximum_trades_per_day} trades/day maximum · stop after ${s.maximum_losing_trades_per_day} losing trades · daily profit lock ${money(s.daily_profit_lock_usd)} · safety reserve ${money(s.safety_reserve_usd)} · force flat ${esc(s.forced_close_ny)} NY.<br>No trade: stale/incomplete session, unrecognized roll, size below one micro, insufficient MLL/DLL room, or spread stress above preset.<br><b>Historical calendar:</b> ${esc(s.event_filter)}`}
function paperTime(v){if(v==null)return '—';const d=typeof v==='number'?new Date(v*1000):new Date(v);return Number.isNaN(d.getTime())?'—':d.toLocaleString()}
async function loadPaper(){try{paperState=await getJSON('/api/lucid-lab/paper/state');renderPaper()}catch(e){$('paper-status').className='pill bad';$('paper-status').textContent='ERROR';$('paper-truth').className='notice error';$('paper-truth').textContent='Paper runtime could not load: '+e.message}}
function renderPaper(){const p=paperState;if(!p||!p.running){$('paper-status').className='pill bad';$('paper-status').textContent='NOT STARTED';$('paper-truth').className='notice error';$('paper-truth').textContent='The server has not started the Lucid paper runtime.';$('paper-toggle').disabled=true;return}const blocked=p.live_order_routing||!p.paper_only;$('paper-status').className='pill '+(p.status==='SCANNING'?'good':p.status==='BREACHED'?'bad':'warn');$('paper-status').textContent=p.status;$('paper-toggle').disabled=Boolean(p.passed||p.breached);$('paper-toggle').textContent=p.enabled?'Pause entries':'Enable entries';$('paper-truth').className='notice'+(blocked?' error':'');$('paper-truth').innerHTML=blocked?'<strong>SAFETY ERROR.</strong> Runtime does not report paper-only mode.':`<strong>PAPER ONLY.</strong> Shared ${money(p.starting_balance)} account; no broker credentials or live order routing. ${esc(p.forward_evidence?.note||'Paper results are not proof of profitability.')}`;const metrics=[['Balance',money(p.balance),'realized ledger',''],['Equity',money(p.equity),'including open marks',''],['Net P&L',money(p.net_pnl),'from '+money(p.starting_balance),Number(p.net_pnl)>=0?'good':'bad'],['Drawdown floor',money(p.floor),p.trail_locked?'locked':'EOD trailing','warn'],['Room to floor',money(p.drawdown_room),'after open marks',Number(p.drawdown_room)>0?'good':'bad'],['Target remaining',money(p.target_remaining),'target '+money(p.target_balance),''],['Today P&L',money(p.day_pnl),p.day_trades+' trade(s)',Number(p.day_pnl)>=0?'good':'bad'],['Open micros',p.open_micros+' / '+p.max_micros,'one shared cap',''],['Committed risk',money(p.committed_stop_risk),'open stops','warn'],['Closed trades',p.forward_evidence?.closed_trades??0,(p.forward_evidence?.evidentiary_trades??0)+' evidentiary',''],['Trading days',p.trading_days,'minimum-day rule applies',''],['Evidence',p.forward_evidence?.status||'COLLECTING','independent audit required','warn']];$('paper-metrics').innerHTML=metrics.map(x=>`<div class="metric ${x[3]}"><div class="k">${esc(x[0])}</div><div class="v">${esc(x[1])}</div><div class="s">${esc(x[2])}</div></div>`).join('');$('paper-feed').textContent=(p.feed_realtime?'REALTIME: ':'BLOCKED: ')+(p.feed_error||p.feed_status);$('paper-calendar').textContent=p.calendar_status||'calendar unavailable';$('paper-events').textContent=p.event_filter_status||'event-filter status unavailable';$('paper-sleeves').innerHTML=(p.sleeves||[]).map(s=>`<article class="paper-sleeve"><div class="eyebrow">${esc(s.instrument)} · ${money(s.risk_budget)} risk · ${esc(s.reward_risk)}R</div><h3>${esc(s.name)}</h3><div class="sleeve-state">${esc(s.status)}</div><div class="meta"><span>Open</span><b>${s.open_positions}</b></div><div class="meta"><span>Closed</span><b>${s.closed_trades}</b></div><div class="meta"><span>Net P&L</span><b>${money(s.net_pnl)}</b></div></article>`).join('')||'<div class="empty">No strategy sleeves loaded.</div>';$('paper-positions').innerHTML=(p.positions||[]).map(r=>`<tr><td>${esc(r.name)}</td><td>${esc(r.side.toUpperCase())} ${r.quantity} ${esc(r.instrument)}</td><td class="num">${esc(r.entry)}</td><td class="num">${esc(r.stop)}</td><td class="num">${esc(r.target)}</td><td class="num">${money(r.unrealized_pnl)}</td><td class="num">${money(r.risk_reserved)}</td></tr>`).join('')||'<tr><td colspan="7" class="empty">No open paper positions.</td></tr>';$('paper-history').innerHTML=(p.history||[]).map(r=>`<tr><td>${esc(paperTime(r.closed_at))}</td><td>${esc(r.name)}</td><td>${esc(r.side.toUpperCase())} ${r.quantity} ${esc(r.instrument)}</td><td class="num">${esc(r.entry)}</td><td class="num">${esc(r.exit)}</td><td class="num">${money(r.net_pnl)}</td><td>${esc(r.reason)}</td><td>${r.evidentiary?'eligible':'excluded'}${r.evidence_note?'<br><small>'+esc(r.evidence_note)+'</small>':''}</td></tr>`).join('')||'<tr><td colspan="8" class="empty">No closed paper trades yet.</td></tr>';$('paper-log').innerHTML=(p.log||[]).map(r=>`<div><time>${esc(paperTime(r.t))}</time><span class="${esc(r.kind)}">${esc(r.msg)}</span></div>`).join('')||'<div><time>—</time><span>No paper activity yet.</span></div>';if(!document.activeElement||document.activeElement!==$('committed-risk')){$('committed-risk').value=p.committed_stop_risk||'0';$('open-cap').value=p.open_micros||0}}
async function togglePaper(){if(!paperState)return;try{await getJSON('/api/lucid-lab/paper/toggle',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({enabled:!paperState.enabled})});await loadPaper()}catch(e){alert('Paper toggle failed: '+e.message)}}
async function resetPaper(){if(!confirm('Reset the Lucid paper account to $25,000 and erase its paper trade history?'))return;try{await getJSON('/api/lucid-lab/paper/reset',{method:'POST'});await loadPaper()}catch(e){alert('Paper reset failed: '+e.message)}}
function setCalculatorDefaults(){const a=state.account,s=state.evidence.selected,initialFloor=Number(a.starting_balance)-Number(a.max_loss);$('plan-balance').value=a.starting_balance;$('plan-floor').value=initialFloor;$('starting-balance').value=a.starting_balance;$('selector-current').value=a.starting_balance;$('selector-remaining').value=a.max_loss;$('current').value=a.starting_balance;$('floor').value=initialFloor;$('daily-room').value=a.daily_loss_limit||'';$('risk-budget').value=Number(a.account_size)===25000?s.morning_risk_usd:Math.round(Number(a.max_loss)*.4);$('reserve').value=s.safety_reserve_usd;if(!$('committed-risk').dataset.edited)$('committed-risk').value='0'}
function syncSelectorToCalculator(){const current=Number($('selector-current').value),remaining=Math.max(0,Number($('selector-remaining').value));if(Number.isFinite(current)&&Number.isFinite(remaining)){$('current').value=current.toFixed(2);$('floor').value=(current-remaining).toFixed(2)}}
async function calculate(){const body={program:$('program').value,stage:'evaluation',account_size:Number($('size').value),daily_drawdown:$('drawdown').value,daily_loss_enabled:$('dll').checked,instrument:$('instrument').value,current_balance:$('current').value,drawdown_floor:$('floor').value,daily_loss_remaining:$('daily-room').value,stop_ticks:$('stop-ticks').value,risk_budget:$('risk-budget').value,safety_reserve:$('reserve').value,open_micro_equivalents:$('open-cap').value,committed_stop_risk:$('committed-risk').value,execution_preset:$('preset').value};try{$('calc-result').innerHTML='<div class="skeleton"></div>';const j=await getJSON('/api/lucid-lab/position-size',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}),r=j.result;$('calc-result').innerHTML=`<div class="eyebrow">Final allowed quantity</div><div class="quantity">${r.final_quantity}</div><div class="meta"><span>Tick value</span><b>${money(r.tick_value)}</b></div><div class="meta"><span>Commission round trip</span><b>${money(r.commission_round_trip)}</b></div><div class="meta"><span>Spread / contract</span><b>${money(r.spread_cost_per_contract)}</b></div><div class="meta"><span>Slippage / contract</span><b>${money(r.slippage_cost_per_contract)}</b></div><div class="meta"><span>Committed stop risk</span><b>${money(r.committed_stop_risk)}</b></div><div class="meta"><span>Usable risk buffer</span><b>${money(r.usable_risk_buffer)}</b></div><div class="meta"><span>Risk / contract</span><b>${money(r.risk_per_contract)}</b></div><div class="meta"><span>Max by account cap</span><b>${r.maximum_by_account_cap}</b></div><div class="meta"><span>Max by risk</span><b>${r.maximum_by_risk}</b></div><div class="meta"><span>Expected execution cost</span><b>${money(r.expected_cost)}</b></div><div class="meta"><span>Loss if stopped</span><b>${money(r.loss_if_stopped)}</b></div><div class="meta"><span>Buffer after stop</span><b>${money(r.remaining_buffer_after_stop)}</b></div><p class="chart-summary">Binding constraint: ${esc(r.binding_constraint)}${r.warnings.length?' · '+esc(r.warnings.join(' ')):''}</p>`}catch(e){$('calc-result').innerHTML=`<div class="notice error">${esc(e.message)}</div>`}}
function planBody(){return{program:$('program').value,stage:'evaluation',account_size:Number($('size').value),daily_drawdown:$('drawdown').value,daily_loss_enabled:$('dll').checked,current_balance:$('plan-balance').value,drawdown_floor:$('plan-floor').value,sessions_remaining:Number($('plan-sessions').value),trades_per_session:Number($('plan-trades').value),loss_streak_tolerance:Number($('plan-streak').value),reward_to_risk:$('plan-rr').value,safety_reserve:$('reserve').value||100}}
async function runPlan(){try{$('plan-result').innerHTML='<div class="skeleton"></div>';
  const j=await getJSON('/api/lucid-lab/plan',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(planBody())}),r=j.result;
  const ok=r.target_reachable,head=ok?'On the arithmetic alone, the target is still reachable.':'The target is no longer reachable on this plan.';
  const rows=[['Still needed',money(r.profit_still_required),'to reach the profit target'],['Average per day',money(r.required_average_session),`over ${num(r.sessions_remaining)} remaining days`],['Most you may risk per trade',money(r.max_risk_per_trade),`limited by the ${esc(r.binding_limit)}`],['Losses before breach',num(r.losses_until_breach),'at that risk, back to back'],['Drawdown room',money(r.drawdown_room),'after the safety reserve'],['Best case left',money(r.best_case_remaining),`if all ${num(r.planned_trades_remaining)} planned trades win`]];
  $('plan-result').innerHTML=`<p class="headline ${ok?'ok':'no'}">${esc(head)}</p><div class="kpi">${rows.map(x=>`<div><div class="kl">${esc(x[0])}</div><div class="kv">${esc(x[1])}</div><div class="kd">${esc(x[2])}</div></div>`).join('')}</div>${r.warnings.length?`<ul style="margin:14px 0 0;padding-left:18px;color:#ffd478">${r.warnings.map(w=>`<li>${esc(w)}</li>`).join('')}</ul>`:''}`}
  catch(e){$('plan-result').innerHTML=`<div class="notice error">${esc(e.message)}</div>`}}
async function runCompliance(trades){try{$('compliance-result').innerHTML='<li><div class="skeleton"></div></li>';
  let parsed=trades;
  if(!parsed){try{parsed=JSON.parse($('compliance-input').value)}catch(err){throw new Error('That is not valid JSON. Expected a list of trades with entry, exit and net_pnl.')}}
  if(!Array.isArray(parsed))throw new Error('Expected a JSON list of trades.');
  const j=await getJSON('/api/lucid-lab/compliance',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({program:$('program').value,stage:'evaluation',account_size:Number($('size').value),daily_drawdown:$('drawdown').value,daily_loss_enabled:$('dll').checked,trades:parsed})}),r=j.result;
  if(!r.checks.length){$('compliance-result').innerHTML='<li><div class="empty">Nothing to score.</div></li>';return}
  $('compliance-result').innerHTML=r.checks.map(c=>{const v=c.value_pct!=null?c.value_pct+'%':num(c.value),lim=c.limit_pct!=null?c.limit_pct+'%':num(c.limit);
    const pctNum=c.value_pct!=null?Number(c.value_pct):(Number(c.limit)>0?Number(c.value)/Number(c.limit)*100:(Number(c.value)>0?100:0));
    const cap=Math.max(0,Math.min(100,pctNum));
    return `<li><div class="row"><span class="st ${c.breached?'bad':'ok'}">${c.breached?'BREACH':'OK'}</span><span class="name">${esc(c.rule)}</span><span class="tag ${c.confidence==='verified'?'':'estimated'}">${esc(c.confidence)}</span><span class="val">${esc(v)} / ${esc(lim)}</span></div><div class="meas">${esc(c.measure)}${c.note?' — '+esc(c.note):''}</div><div class="bar-meter ${c.breached?'bad':''}"><i style="width:${cap}%"></i></div></li>`}).join('')
  +`<li><div class="meas">Scored ${num(r.trades_scored)} trade(s) and ${num(r.orders_scored)} order timestamp(s). A rule marked <b>estimated</b> is a conservative local threshold because Lucid publishes no number for it.</div></li>`}
  catch(e){$('compliance-result').innerHTML=`<li><div class="notice error">${esc(e.message)}</div></li>`}}
async function complianceFromPaper(){try{const j=await getJSON('/api/lucid-lab/paper/state');const hist=(j.history||[]).filter(h=>h.entry_ts&&h.exit_ts);
  if(!hist.length){$('compliance-result').innerHTML='<li><div class="empty">The paper runtime has no closed trades with timestamps yet.</div></li>';return}
  await runCompliance(hist.map(h=>({entry:h.entry_ts,exit:h.exit_ts,net_pnl:h.pnl??0,instrument:h.instrument||h.market||'',side:h.side||'long'})))}
  catch(e){$('compliance-result').innerHTML=`<li><div class="notice error">${esc(e.message)}</div></li>`}}
function renderPlan(){$('daily-plan').innerHTML=state.daily_plan.map(p=>`<div class="plan-step"><b>${esc(p.phase)}</b><ul>${p.checks.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`).join('')}
function activeHorizon(){return state.evidence.horizons[$('horizon').value]||state.evidence.horizons['45']}
function renderBacktest(){const e=state.evidence,r=activeHorizon(),t=e.trade_statistics,c=t.concentration,applies=state.evidence_applies,d=r.duration,interval=r.pass_exact_binomial_95;$('backtest-caption').textContent=(applies?'Rules match this LucidPro EOD configuration, but the verdict is NO-GO. ':'Reference evidence only for this selection. ')+'Headline rates use '+r.windows+' disjoint '+$('horizon').value+'-session blocks from Dukascopy proxy data; overlapping starts are diagnostic only.';const duration=`fast ${d.fastest} · P10 ${Number(d.p10).toFixed(1)} · P25 ${Number(d.p25).toFixed(1)} · mean ${Number(d.mean).toFixed(1)} · P75 ${Number(d.p75).toFixed(1)} · P90 ${Number(d.p90).toFixed(1)}`;const uncertainty=interval?'Exact small-sample interval '+pct(interval[0])+'–'+pct(interval[1]):'No interval stored';const items=[['Disjoint-block pass rate',applies?pct(r.pass_rate):'scope mismatch',uncertainty,'warn'],['Open-equity breach',applies?pct(r.breach_rate):'scope mismatch',r.breaches+' of '+r.windows+' blocks, every open position marked each minute. A zero is structural: risk-capped sizing bounds the loss, so it is a property of the sizing rule rather than proof of safety','warn'],['Unfinished',applies?pct(r.unfinished_rate):'scope mismatch',r.unfinished+' blocks','warn'],['Median to pass*',d.median?d.median+' sessions':'—',duration,''],['Restricted mean',Number(d.restricted_mean).toFixed(1)+' sessions','all blocks; horizon-censored',''],['Test trades',num(t.trades),'from '+e.data.test_trades_raw+' raw signals',''],['Net expectancy',money(t.expectancy),'standalone sized trade; not a pass forecast','warn'],['Profit factor',t.profit_factor,'after modeled costs',''],['Worst minute-marked drawdown',money(r.worst_max_drawdown),'adverse minute extremes; shared account','bad'],['Commission',money(t.commissions),'official per-side rate',''],['Spread cost',money(t.spread_cost),'modeled',''],['Slippage cost',money(t.slippage_cost),'modeled',''],['Costs / positive gross',t.cost_pct_of_positive_gross+'%',money(t.positive_gross_profit)+' positive gross',''],['Largest winning trade',c.largest_winning_trade_share_pct+'%','share of all positive gross',''],['Largest positive day',c.largest_positive_day_share_pct+'%','share of positive daily P&L',''],['Largest positive month',c.largest_positive_month_share_pct+'%','share of positive monthly P&L','']];const FLAG={good:'FAVOURABLE',warn:'CAUTION',bad:'ADVERSE'};$('backtest-metrics').innerHTML=items.map(x=>`<div class="metric ${x[3]}"><div class="k">${esc(x[0])}${x[3]?`<span class="flag">${FLAG[x[3]]}</span>`:''}</div><div class="v">${esc(x[1])}</div><div class="s">${esc(x[2])}</div></div>`).join('');$('split-table').innerHTML=Object.entries(e.split_results).map(([k,v])=>`<tr><td>${esc(k.replaceAll('_',' '))}</td><td class="num">${num(v.windows)}</td><td class="num">${pct(v.pass_rate)}</td><td class="num">${pct(v.breach_rate)}</td><td class="num">${pct(v.unfinished_rate)}</td><td class="num">${v.duration.median??'—'}</td><td class="num">${money(v.worst_max_drawdown)}</td></tr>`).join('')}
function renderStress(){$('stress-table').innerHTML=state.evidence.stresses.map(r=>`<tr class="${r.id===$('preset').value?'selected-row':''}"><td>${esc(r.label)}${r.selection?`<br><small>${esc(r.selection)}</small>`:''}</td><td class="num">${num(r.windows)}</td><td class="num">${pct(r.pass_rate)}</td><td class="num">${pct(r.breach_rate)}</td><td class="num">${pct(r.unfinished_rate)}</td><td class="num">${r.duration?.median??'—'}</td><td class="num">${money(r.worst_max_drawdown)}</td></tr>`).join('')}
function lineChart(id,values,fn,color,label){const svg=$(id);if(!values.length){svg.innerHTML='';svg.setAttribute('aria-label','No '+label+' data');return}const w=600,h=160,p=12,ys=values.map(fn),lo=Math.min(...ys),hi=Math.max(...ys),span=hi-lo||1,pts=ys.map((v,i)=>`${p+i*(w-2*p)/Math.max(1,ys.length-1)},${h-p-(v-lo)*(h-2*p)/span}`).join(' ');svg.setAttribute('viewBox',`0 0 ${w} ${h}`);svg.setAttribute('aria-label',`${label}: minimum ${lo.toFixed(2)}, maximum ${hi.toFixed(2)}, ${values.length} observations`);svg.innerHTML=`<line x1="${p}" y1="${h-p}" x2="${w-p}" y2="${h-p}" stroke="#334155"/><polyline fill="none" stroke="${color}" stroke-width="3" points="${pts}"/><circle cx="${pts.split(' ').at(-1).split(',')[0]}" cy="${pts.split(' ').at(-1).split(',')[1]}" r="4" fill="${color}"/>`}
function barName(r){return String(r.name??r.days??(r.lo!=null?money(r.lo)+'–'+money(r.hi):''))}
function bars(id,rows,value,label,format){const box=$(id),fmt=format||(v=>num(v)),max=Math.max(1,...rows.map(value));
  // Values are published as text as well as height: a tooltip that only opens on
  // hover is unreachable by keyboard and invisible to a screen reader.
  const pairs=rows.map(r=>({name:barName(r),text:fmt(value(r))}));
  box.setAttribute('aria-label',label+'. '+(pairs.length?pairs.map(p=>`${p.name} ${p.text}`).join('; '):'no data'));
  box.innerHTML=rows.map((r,i)=>`<div class="bar" style="height:${Math.max(2,value(r)/max*100)}%" data-tip="${esc(pairs[i].name)}: ${esc(pairs[i].text)}"></div>`).join('');
  const listId=id+'-values';let list=$(listId);
  if(!list){list=document.createElement('ul');list.id=listId;list.className='bar-values';box.insertAdjacentElement('afterend',list)}
  list.innerHTML=pairs.map(p=>`<li><span>${esc(p.name)}</span><b>${esc(p.text)}</b></li>`).join('')}
function renderCharts(){const e=state.evidence,c=e.charts,t=c.representative_timeline,mc=e.monte_carlo;lineChart('equity-chart',t,x=>Number(x.ending_balance),'#35d69f','Representative ending balance');lineChart('drawdown-chart',t,x=>Number(x.remaining_drawdown),'#fb7185','Remaining drawdown room');lineChart('rolling-chart',c.rolling_pass_probability,x=>Number(x.pass_rate)*100,'#f4bd4a','Rolling pass probability percent');$('equity-summary').textContent=t.length?`Starts ${money(t[0].starting_balance)} and ends ${money(t.at(-1).ending_balance)} after ${t.length} sessions.`:'No representative path.';$('drawdown-summary').textContent=t.length?`Minimum displayed remaining room ${money(Math.min(...t.map(x=>x.remaining_drawdown)))}.`:'No drawdown path.';$('rolling-summary').textContent=`${c.rolling_pass_probability.length} chronological blocks; this exposes regime variation.`;bars('duration-bars',c.pass_duration_histogram,x=>x.count,'Conditional pass-time histogram',v=>num(v)+' passes');$('duration-summary').textContent='Successful paths only; non-passes are not silently treated as missing.';bars('outcome-bars',c.outcome_distribution,x=>x.count,'All historical path outcomes',v=>num(v)+' blocks');$('outcome-summary').textContent=c.outcome_distribution.map(x=>x.name+' '+num(x.count)).join(' · ')+'. Every start remains in the denominator.';bars('terminal-bars',mc.terminal_profit_histogram,x=>x.count,'Monte Carlo terminal profit histogram',v=>num(v)+' paths');$('terminal-summary').textContent=`Monte Carlo median ${money(mc.terminal_profit_median)}; P05 ${money(mc.terminal_profit_p05)}; P95 ${money(mc.terminal_profit_p95)}.`;bars('cost-bars',c.cost_breakdown,x=>Number(x.value),'Modeled cost breakdown',v=>money(v));$('cost-summary').textContent=c.cost_breakdown.map(x=>x.name+' '+money(x.value)).join(' · ')}
function renderTimeline(){$('timeline').innerHTML=state.evidence.charts.representative_timeline.map(r=>`<tr><td>${r.day}</td><td>${esc(r.session)}</td><td class="num">${money(r.starting_balance)}</td><td class="num">${money(r.ending_balance)}</td><td class="num">${money(r.daily_net_pnl)}</td><td class="num">${money(r.largest_profitable_day)}</td><td class="num">${r.consistency_pct==null?'—':r.consistency_pct+'%'}</td><td class="num">${money(r.drawdown_floor)}</td><td class="num">${money(r.remaining_drawdown)}</td><td class="num">${esc(r.permitted_contracts)}</td><td>${esc(r.status)}${r.warning?'<br><small>'+esc(r.warning)+'</small>':''}</td></tr>`).join('')}
function renderCandidates(){const e=state.evidence;$('candidates').innerHTML=e.candidates.map(r=>`<tr class="${r.id==='selected_portfolio'?'selected-row':''}"><td><b>${esc(r.name)}</b><br><small>${esc(r.strategy_version)}</small></td><td>${esc(r.instrument)}</td><td class="num">${num(r.trades)}</td><td class="num">${num(r.windows)}</td><td class="num">${money(r.net_expectancy)}</td><td class="num">${r.profit_factor??'—'}</td><td class="num">${pct(r.normal_pass_rate)}</td><td class="num">${pct(r.stressed_pass_rate)}</td><td><span class="tag ${r.validation_status==='INVALIDATED'?'disputed':''}">${esc(r.validation_status)}</span><br><small>${esc(r.reason)}</small></td></tr>`).join('');const rows=e.sensitivity.filter(x=>x.prior_risk===100);bars('sensitivity-bars',rows,x=>Number(x.pass_rate)*100,'Pass probability across morning risk sensitivity',v=>Number(v).toFixed(1)+'%');$('sensitivity-summary').textContent=rows.map(x=>money(x.morning_risk)+' → '+pct(x.pass_rate)+' pass / '+pct(x.breach_rate)+' breach').join(' · ');$('signal-sensitivity').innerHTML=e.signal_sensitivity.map(r=>`<tr class="${r.selected?'selected-row':''}"><td>${esc(r.dimension.replaceAll('_',' '))}</td><td class="num">${esc(r.value)}</td><td>${r.selected?'selected':'alternative'}</td><td class="num">${num(r.trades)}</td><td class="num">${money(r.expectancy)}</td><td class="num">${pct(r.pass_rate)}</td><td class="num">${pct(r.breach_rate)}</td><td class="num">${pct(r.unfinished_rate)}</td></tr>`).join('')}
function renderMethod(){const e=state.evidence,m=e.methodology,mc=e.monte_carlo,rc=e.risk_controls,t=e.trade_statistics,c=t.concentration,interval=e.horizons['45'].pass_exact_binomial_95;const years=e.walk_forward.map(r=>`${r.year}: ${pct(r.pass_rate)} pass / ${pct(r.unfinished_rate)} unfinished (${num(r.windows)} disjoint blocks)`).join(' · ');const versions=e.candidates.map(r=>`${r.strategy_version}: ${r.validation_status}`).join(' · ');const gates=e.verdict.validation_gates.map(g=>`${g.passed?'PASS':'FAIL'} ${g.id}: ${g.detail}`).join('<br>');const cs=e.candidate_search;const search=cs?`<h3>Pre-registered candidate search</h3><p>A frozen grid of ${num(cs.variants_tested)} variants (registration ${esc(cs.preregistration_sha)}, run ${esc(cs.run_id)}) was searched on development data, confirmed once on validation and opened once on the holdout. The best candidate reached ${pct(cs.best_candidate_holdout_pass_rate)} over ${num(cs.holdout_blocks)} disjoint holdout blocks against a frozen baseline of ${pct(cs.frozen_baseline_holdout_pass_rate)}, so it <b>${cs.beat_frozen_baseline?'beat':'did not beat'}</b> the baseline. Its multiplicity-corrected interval is ${pct(cs.best_candidate_multiplicity_95[0])}–${pct(cs.best_candidate_multiplicity_95[1])}. Search decision: <b>${esc(cs.decision)}</b>.</p>`:'';$('methodology').innerHTML=`<h3>Decision</h3><p><b>${esc(e.verdict.decision)}</b> — ${esc(e.verdict.reason)}</p><p>${gates}</p>${search}<h3>Data and splits</h3><p>${esc(e.data.source)}. ${esc(m.development)}; validation ${esc(m.validation)}; chronological test ${esc(m.test)}. ${esc(m.selection_warning)}</p><h3>Execution</h3><p>${esc(m.fill)}. ${esc(m.commission)}. OHLC bars cannot establish queue position or actual futures bid/ask fills.</p><h3>Walk-forward years</h3><p>${esc(years)}</p><h3>Concentration</h3><p>Largest winning trade ${c.largest_winning_trade_share_pct}% of positive gross; largest positive day ${c.largest_positive_day_share_pct}%; largest positive month ${c.largest_positive_month_share_pct}%. Trades by year: ${esc(JSON.stringify(c.trades_by_year))}.</p><h3>Uncertainty</h3><p>${esc(m.uncertainty)}. Exact disjoint-block interval ${pct(interval[0])}–${pct(interval[1])}. Resampling ${num(mc.paths)} paths: ${pct(mc.pass_rate)} pass, ${pct(mc.breach_rate)} breach, ${pct(mc.unfinished_rate)} unfinished. ${esc(mc.warning)}</p><h3>Version history</h3><p>${esc(versions)}</p><h3>Risk controls observed</h3><p>${num(rc.open_equity_checks)} minute open-equity checks, ${num(rc.intraday_open_equity_breaches)} open-equity breaches, ${num(rc.risk_rejections)} entry rejections, ${num(rc.contract_cap_limits)} cap limits, ${num(rc.strategy_rule_blocks)} daily-strategy blocks and ${num(rc.daily_loss_blocks)} DLL blocks across disjoint primary paths.</p><h3>Known conflicts</h3><p>The historical event filter was not applied because no point-in-time calendar archive is present. The source data are Dukascopy CFD/index proxies, not exchange futures trades or depth. Therefore the result is NO-GO even if its descriptive pass rate is positive.</p>`;$('sources').innerHTML=state.account.sources.map(s=>`<div class="source-item"><a href="${esc(s.url)}" target="_blank" rel="noopener">${esc(s.title)}</a><small>Displayed ${esc(s.displayed_date)} · retrieved ${esc(s.retrieved_at)}</small></div>`).join('')}
async function startReplay(){try{const j=await getJSON('/api/lucid-lab/simulations',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({execution_preset:$('preset').value,account_size:Number($('size').value)})});activeJob=j.id;$('cancel-btn').disabled=false;pollReplay()}catch(e){$('run-state').textContent='error: '+e.message}}
async function pollReplay(){if(!activeJob)return;try{const j=await getJSON('/api/lucid-lab/simulations/'+encodeURIComponent(activeJob));$('progress').style.width=j.progress+'%';$('progress-track').setAttribute('aria-valuenow',String(j.progress));$('run-state').textContent=j.status+' · '+j.progress+'%';if(['completed','error','cancelled'].includes(j.status)){$('cancel-btn').disabled=true;activeJob=null;return}pollTimer=setTimeout(pollReplay,150)}catch(e){$('run-state').textContent='error: '+e.message}}
async function cancelReplay(){if(!activeJob)return;clearTimeout(pollTimer);await fetch('/api/lucid-lab/simulations/'+encodeURIComponent(activeJob)+'/cancel',{method:'POST'});$('run-state').textContent='cancelled';$('cancel-btn').disabled=true;activeJob=null}
async function validateUpload(ev){ev.preventDefault();const file=$('data-file').files[0];if(!file)return;const form=new FormData();form.append('file',file);$('upload-result').className='empty';$('upload-result').textContent='Validating…';try{const j=await getJSON('/api/lucid-lab/validate-data?symbol='+encodeURIComponent($('data-symbol').value),{method:'POST',body:form});const r=j.report;$('upload-result').className=r.ok?'notice':'notice error';$('upload-result').textContent=(r.ok?'Valid':'Rejected')+`: ${r.rows} rows across ${r.session_count} session(s), ${r.first_timestamp||'—'} → ${r.last_timestamp||'—'}; ${r.missing_intervals} gaps, ${r.zero_volume_rows} zero-volume rows, ${r.incomplete_rth_sessions} incomplete RTH sessions, ${r.contract_rollovers} rollovers. `+[...r.errors,...r.warnings].join(' ')}catch(e){$('upload-result').className='notice error';$('upload-result').textContent=e.message}}
$('program').addEventListener('change',programChanged);$('size').addEventListener('change',loadSnapshot);$('drawdown').addEventListener('change',loadSnapshot);$('dll').addEventListener('change',loadSnapshot);$('horizon').addEventListener('change',()=>{renderBacktest()});$('preset').addEventListener('change',()=>{$('preset').dataset.keep=$('preset').value;renderStress()});$('selector-current').addEventListener('input',syncSelectorToCalculator);$('selector-remaining').addEventListener('input',syncSelectorToCalculator);$('committed-risk').addEventListener('input',()=>{$('committed-risk').dataset.edited='true'});$('calc-btn').addEventListener('click',calculate);$('plan-btn').addEventListener('click',runPlan);$('compliance-btn').addEventListener('click',()=>runCompliance(null));$('compliance-paper-btn').addEventListener('click',complianceFromPaper);$('paper-toggle').addEventListener('click',togglePaper);$('paper-reset').addEventListener('click',resetPaper);$('replay-btn').addEventListener('click',startReplay);$('cancel-btn').addEventListener('click',cancelReplay);$('upload-form').addEventListener('submit',validateUpload);
loadSnapshot();loadPaper();setInterval(loadPaper,2000);
</script>
</body></html>"""
